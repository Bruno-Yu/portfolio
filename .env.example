# ===========================================
# Bruno Portfolio - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to version control

# ===========================================
# System Name
# ===========================================
# Display name for the website (shown in browser tab, headers, etc.)
VITE_SYSTEM_NAME=Bruno Portfolio

# ===========================================
# Cloudflare Workers (Backend)
# ===========================================
# Get these from Cloudflare Dashboard:
# - API Token: Dashboard → API Tokens → Create Token
# - Account ID: Dashboard → Workers & Pages → Settings
# - D1 Database ID: Dashboard → D1 → Your Database

CLOUDFLARE_API_TOKEN=your_api_token_here
CLOUDFLARE_ACCOUNT_ID=your_account_id_here
CLOUDFLARE_D1_DATABASE_ID=your_database_id_here

# ===========================================
# Admin Authentication (Environment Variable Super Admin)
# ===========================================
# These are "break-glass" credentials that bypass the database
# Use when database is unavailable or for emergency access
#
# Setting methods:
#   - Local: Add to .env.local
#   - Production: Use wrangler secrets
#     npx wrangler secret put ADMIN_USERNAME
#     npx wrangler secret put ADMIN_PASSWORD
#
# Password can be:
#   - Plain text (for development convenience)
#   - Bcrypt hash (recommended for production)
#     Generate with: npx bcrypt hash your_password 10

# Super admin username (compared first during login)
ADMIN_USERNAME=admin

# Super admin password (bcrypt hash or plain text)
# Example bcrypt: $2b$10$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ADMIN_PASSWORD=your_password_or_bcrypt_hash

# JWT secret key for signing tokens
# Must be a long random string (32+ characters recommended)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# ===========================================
# Frontend (Vite)
# ===========================================
# VITE_* variables are exposed to the browser
# Set in .env.local for local development
# Set in Cloudflare Pages Settings for production

# Local Workers API URL (development)
VITE_API_PREFIX=http://localhost:8787

# Production Workers API URL (uncomment for production deployment)
# VITE_API_PREFIX=https://bruno-portfolio-api.YOUR_USERNAME.workers.dev

# ===========================================
# Deployment Paths (Optional)
# ===========================================
# Repository name for GitHub Pages deployment
VITE_REPO_NAME=portfolio

# Base path for the application (use '/' for root or '/portfolio' for subdirectory)
# For local development with GitHub Pages, set to '/portfolio'
# For production deployment, adjust accordingly
VITE_BASE_PATH=/

# ===========================================
# GitHub Pages Configuration
# ===========================================
# Homepage URL for package.json (uncomment for GitHub Pages)
# VITE_HOMEPAGE=https://bruno-yu.github.io/portfolio
