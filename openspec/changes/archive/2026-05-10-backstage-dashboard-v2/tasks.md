## 1. Database Migration

- [x] 1.1 Create migration file `workers/migrations/0003_content_experiences_messages.sql` — implements the "Content Storage: Key-Value `content_blocks` + Structured `experiences`" design decision: add `content_blocks (key TEXT, lang TEXT, value TEXT, updated_at INTEGER, PRIMARY KEY(key, lang))` table for flat bilingual text, `experiences` table (id, sort_order, from_year, to_year, role_en, role_zh, org_en, org_zh, location, tags, bullets_en, bullets_zh, note_en, note_zh) for structured work history, and `messages` table (id, sender_name, sender_email NOT NULL, subject, body NOT NULL, is_read INTEGER DEFAULT 0, created_at INTEGER); verifies "content_blocks table stores bilingual text by key" and "experiences table stores structured bilingual work history" and "messages table stores contact inquiries"
- [x] 1.2 Create Drizzle schema `workers/src/drizzle/schema/contentBlocks.ts` — export `contentBlocks` table definition with `primaryKey([key, lang])` composite PK; add to `workers/src/drizzle/schema/index.ts` barrel export
- [x] 1.3 Create Drizzle schema `workers/src/drizzle/schema/experiences.ts` — export `experiences` table with all bilingual columns, `location`, JSON-backed `tags`, JSON-backed `bulletsEn` / `bulletsZh`, and `sortOrder`; add to barrel export
- [x] 1.4 Create Drizzle schema `workers/src/drizzle/schema/messages.ts` — export `messages` table; add to barrel export
- [x] 1.5 Apply migration locally with `wrangler d1 migrations apply bruno-portfolio-db --local` and verify all three tables exist with correct columns

## 2. Backend API — content-blocks Route

- [x] 2.1 Create `workers/src/routes/contentBlocks.ts` — implement `GET /` handler: select all rows from `content_blocks`, return flat map `{ "<key>.<lang>": value }` with no auth requirement; fulfills "content-blocks GET endpoint is public"
- [x] 2.2 Add `POST /` handler in `contentBlocks.ts` using `authMiddleware` + `requireAdmin`: accept `{ blocks: [{ key, lang, value }] }` body validated with Zod (lang must be "en" | "zh"), upsert all rows in a D1 batch statement, return `{ success: true, count: N }`; fulfills "content-blocks POST endpoint is admin-only"
- [x] 2.3 Mount content-blocks route in `workers/src/index.ts`: `app.route('/api/content-blocks', contentBlocksRoutes)` following the existing pattern; fulfills "Admin CSS: Shared Design Tokens, Dedicated Admin Layout File" deployment preparation

## 3. Backend API — experiences Route

- [x] 3.1 Create `workers/src/routes/experiences.ts` — implement public `GET /` handler: select all from `experiences` ordered by `sort_order ASC, id ASC`, return `{ success: true, data: [...] }`; fulfills "experiences table stores structured bilingual work history" GET requirement
- [x] 3.2 Add `POST /` (admin) handler in `experiences.ts`: validate body with Zod (fromYear required, toYear optional, all bilingual fields optional, location optional, tags and bullets arrays optional), insert and return new row with 201; fulfills "experiences table stores structured bilingual work history" CREATE requirement
- [x] 3.3 Add `PUT /:id` (admin) handler in `experiences.ts`: validate partial body, update only provided fields, serialize tags/bullets arrays to JSON, return updated row; fulfills UPDATE requirement
- [x] 3.4 Add `DELETE /:id` (admin) handler in `experiences.ts`: delete row by id, return `{ success: true }`; fulfills DELETE requirement
- [x] 3.5 Mount experiences route in `workers/src/index.ts`: `app.route('/api/experiences', experiencesRoutes)`

## 4. Backend API — Analytics Proxy

- [x] 4.1 Create `workers/src/routes/analytics.ts` — implement admin-only `GET /` handler that reads `CF_ANALYTICS_API_TOKEN` and `CF_ZONE_ID` from `c.env`; if either is missing return `{ success: false, error: { code: "ANALYTICS_NOT_CONFIGURED" } }` with status 503; fulfills "Backend analytics proxy fetches real CF Analytics data" missing credentials scenario
- [x] 4.2 In `analytics.ts`, build the CF Analytics GraphQL query for `httpRequests1dGroups` with `days` query param (default 30, max 90, minimum 1); call `https://api.cloudflare.com/client/v4/graphql` with `Authorization: Bearer <CF_ANALYTICS_API_TOKEN>` header; parse response into `{ totalRequests, uniqueVisitors, daily: [{ date, requests, uniques }] }` and return `{ success: true, data: {...} }`; wrap in try/catch — on error return `{ success: false, error: { code: "ANALYTICS_UNAVAILABLE" } }` with status 502; fulfills "Cloudflare Analytics Proxy: Backend Route, Not Direct Client Call"
- [x] 4.3 Mount analytics route in `workers/src/index.ts`: `app.route('/api/admin/analytics', analyticsRoutes)`
- [x] 4.4 Add `POST /api/auth/change-password` handler in `workers/src/routes/auth.ts`: require auth middleware, validate body `{ currentPassword: string, newPassword: string }` (newPassword ≥ 8 chars, return 400 if shorter), verify currentPassword against stored bcrypt hash (return 401 if wrong), update password hash in users table, return `{ success: true }`; fulfills "Change-password backend endpoint"
- [x] 4.5 Set Wrangler secrets documentation: add `CF_ANALYTICS_API_TOKEN` and `CF_ZONE_ID` to `workers/wrangler.toml` under `[vars]` as empty placeholders with comments; document that secrets must be set with `wrangler secret put CF_ANALYTICS_API_TOKEN` and `wrangler secret put CF_ZONE_ID`

## 5. Admin Design System

- [x] 5.1 Create `frontend/src/styles/design-admin.css` — define `.admin-shell` (grid: 240px 1fr, min-height: 100vh), `.admin-side` (always-dark: background `#0a0805`, color `rgba(245,240,226,0.70)`, no transition on theme change), `.admin-side__brand`, `.admin-nav`, `.admin-nav__group`, `.admin-nav__item` with active state (yellow left border + accent color), `.admin-nav__item .badge` (yellow pill), `.admin-side__foot`, `.admin-user`, `.admin-topbar` (sticky, breadcrumbs + search + actions), `.admin-content` (padding: 40px); uses only existing CSS custom properties from `design-tokens.css`; fulfills "Admin CSS: Shared Design Tokens, Dedicated Admin Layout File" and "design-admin.css uses shared design tokens without Flowbite"
- [x] 5.2 Add data table classes to `design-admin.css`: `.dt-wrap` (1.5px ink border), `.dt` (full-width table), `.dt thead th` (10px mono uppercase, --bg background), `.dt tbody tr:hover` (--bg-sunk background), `.dt .cell-thumb` (56×36px with border), `.dt .cell-title` (bold display font 14.5px), `.dt .cell-sub` (10px mono --ink-3), `.dt .row-actions`, `.icon-btn` (30×30px, 1.5px border, hover: ink fill), `.icon-btn.danger` (red hover border/text), `.status-pill` (10px mono), `.status-pill.is-pub` (yellow bg), `.status-pill.is-draft` (gray); fulfills "Data table component uses .dt-wrap / .dt classes"
- [x] 5.3 Add modal classes to `design-admin.css`: `.modal-overlay` (fixed inset-0, rgba(10,10,8,0.55), display grid, place-items center, z-index 50), `.modal` (max-width 640px, 1.5px ink border, --bg-elev background, fade-in 180ms + rise 220ms animations), `.modal--lg` (max-width 820px), `.modal__head` (padding 22px 26px, border-bottom), `.modal__head h3`, `.modal__close` (32×32px icon button), `.modal__body` (padding 22px 26px, flex column gap 16px), `.modal__foot` (padding 22px 26px, flex justify-between, border-top); add Escape key close via global keydown listener in a shared hook or AdminShell; fulfills "Modal component uses .modal-overlay / .modal classes"
- [x] 5.4 Add form classes to `design-admin.css`: `.field` (display flex, flex-direction column, gap 6px), `.field__label` (10px mono uppercase, --ink-3), `.field input`, `.field textarea`, `.field select` (1.5px --ink border, --bg-elev background, padding 10px 12px, focus: background --accent-y-soft, outline none), `.field textarea` (min-height 110px, resize vertical), `.tag-editor` (flex wrap, 1.5px ink border, --bg-elev), `.tag-editor .tag` + `.tag-editor input`, `.seg` (language toggle: segmented control, active: --ink bg + --bg text), `.field-row` (flex gap for side-by-side fields); fulfills "Form fields use .field / .field__label classes"
- [x] 5.5 Add stat card + panel classes to `design-admin.css`: `.admin-stat-row` (grid, 4 columns, gap 16px; 2 columns at <1100px), `.stat` (--bg-elev background, 1.5px ink border, padding 20px), `.stat .k` (10px mono uppercase), `.stat .v` (36px display font 600), `.stat .delta` (11px mono, green #15803d), `.stat .delta.is-down` (red #b91c1c), `.stat__tick` (top-right label), `.panel` (--bg-elev background, 1.5px ink border), `.panel__head` (padding with border-bottom), `.activity` (timeline list with `.marker-dot` 14px circles, `.marker-dot.is-y` yellow), `.admin-page-head` (display flex, justify-between, align-items start, padding-bottom 24px, border-bottom 1.5px ink; `h1` at 40px display font 600)
- [x] 5.6 Import `design-admin.css` in `frontend/src/main.tsx`; remove Flowbite React import (check `main.tsx` and `App.tsx`); uninstall `flowbite-react` from `frontend/package.json` with `pnpm remove flowbite-react`; verify TypeScript build succeeds with no missing module errors; fulfills "design-admin.css uses shared design tokens without Flowbite"

## 6. AdminShell Layout Component

- [x] 6.1 Create `frontend/src/components/Layout/BackStage/AdminShell.tsx` — renders `.admin-shell` grid containing `.admin-side` (sidebar) and `.admin-main` (topbar + `<Outlet />`); sidebar contains brand section ("BackStage" + current user role; no V2 suffix), `.admin-nav` groups with nav items (icons as SVG inline or via Icon component), `.admin-side__foot` with circular user avatar + name + logout button; uses `useNavigate` for logout redirect; fulfills "AdminShell: Outlet-based Layout, Not HOC" and "Shared admin layout with always-dark sidebar"
- [x] 6.2 In `AdminShell.tsx`, add auth guard on mount: check for JWT token in localStorage (`authState`); if absent or expired (decode and check `exp`), call `navigate('/login', { replace: true })`; fulfills "Auth guard for all backstage routes"
- [x] 6.3 In `AdminShell.tsx` sidebar, fetch works count from `GET /api/works` and display as badge on Works nav item; apply `.is-active` class to nav item matching current pathname via `useLocation()`; fulfills "Admin sidebar navigation"
- [x] 6.4 In `AdminShell.tsx`, render `.admin-topbar` containing breadcrumbs (`BACKSTAGE / <PAGE NAME>` derived from current route), search input (`<input className="admin-search">`), language toggle (reuse existing Redux action), theme toggle (reuse existing Redux action), and "View site" link (`<a href="/" target="_blank">`); fulfills "Admin topbar with breadcrumbs and actions"
- [x] 6.5 Create `frontend/src/components/Layout/BackStage/AdminPageHead.tsx` — renders `.admin-page-head` with `h1` title prop, `p` description prop, and optional `children` (right-aligned CTA button); used by all admin pages
- [x] 6.6 Update `frontend/src/router/index.tsx` to wrap all `/backstage/*` routes under `AdminShell` as parent layout using React Router v6 nested routes; add routes for `overview`, `content`, `works`, `skills`, `users`, `messages`, `settings`; fulfills "Admin router wraps all backstage routes under AdminShell" and "AdminShell: Outlet-based Layout, Not HOC"

## 7. Content Hooks (Frontend Fallback Layer)

- [x] 7.1 Create `frontend/src/hooks/usePageContent.ts` — export `usePageContent()` hook that calls `GET /api/content-blocks` on mount (once per session, cached in module-level `Map<string, string>`), and returns a `get(key: string, lang: 'en' | 'zh'): string` resolver; resolver returns API value if key+lang exists in cache, else returns the static fallback value passed as third argument `get(key, lang, fallback)`; on network error, log warning and use fallback silently; fulfills "Frontend Content Fallback: Hook-Based API-First with Static Fallback" and "Frontend sections fall back to static content when no API override exists"
- [x] 7.2 Create `frontend/src/hooks/useExperiences.ts` — export `useExperiences()` hook that calls `GET /api/experiences`; if the response array is empty, return the `experience` array from `portfolio.ts`; if the API fails, return `portfolio.ts` array silently; fulfills "Frontend sections fall back to static content when no API override exists"
- [x] 7.3 Update `frontend/src/views/FrontStage/Home/sections/HeroSection.tsx` (and equivalent Hero component) to use `usePageContent()` for greeting, headline, and sub-headline with `portfolio.ts` values as fallbacks
- [x] 7.4 Update `frontend/src/views/FrontStage/Home/sections/AboutSection.tsx` (and equivalent) to use `usePageContent()` for about paragraphs with `portfolio.ts` `about.paragraphs` as fallback
- [x] 7.5 Update Experience section component to use `useExperiences()` instead of directly importing `experience` from `portfolio.ts`

## 8. Content Editor Page

- [x] 8.1 Create `frontend/src/views/BackStage/Content/index.tsx` — render page with `AdminPageHead` (title "Content", description), language toggle `<div className="seg">` bound to local `lang` state (not global — avoids changing public site language while editing), and three `.editor-section` blocks with `.editor-section__head` showing number + title; fulfills "Content Editor page renders three editable sections"
- [x] 8.2 In Content editor Section 01 (Hero / Homepage): render three `.field` inputs — greeting (line input, maps to `hero.greeting`), headline (textarea, maps to `hero.headline`), sub-headline (textarea, maps to `hero.sub`); on mount populate from `usePageContent()` with `portfolio.ts` fallbacks `home.hello`, `home.headline`, `home.sub`
- [x] 8.3 In Content editor Section 02 (About): render one `.field` textarea for about paragraphs (maps to `about.paragraphs`, newline-separated); populate from `usePageContent()` with `about.paragraphs[lang].join('\n\n')` as fallback
- [x] 8.4 In Content editor Section 03 (Work Experience): render `.experience-row` for each experience entry with `from_year` input, `to_year` input, role input (language-aware), org input (language-aware), location input, tags CSV field, bullets textarea (language-aware), delete button; "Add experience" button appends blank row; fulfills "Work Experience entries support add, edit, and delete"
- [x] 8.5 Implement sticky save bar at bottom of Content page: `.save-bar` with left section (save status indicator, resets to neutral after 3s) and right section ("Reset" outline button + "Save" button); on Save click: collect all content block changes → `POST /api/content-blocks`; for experience rows: call `POST`, `PUT`, or `DELETE /api/experiences/:id` as needed per row change; on all success set status to "Saved ✓" for 3s; on error display "Save failed" in red; fulfills "Sticky save bar persists and shows save status"
- [x] 8.6 Fix Content page layout polish: sidebar is full `100vh` and visually covers the fixed save bar; save bar action order is Reset then Save; long modal / editor content scrolls within the main content without overflowing controls

## 9. Overview Dashboard Page

- [x] 9.1 Create `frontend/src/views/BackStage/Overview/index.tsx` — render page with `AdminPageHead` (title "Overview"), `.admin-stat-row` with 4 `.stat` cards; fetch from `GET /api/admin/analytics?days=30` and `GET /api/works`; if analytics returns error code `ANALYTICS_UNAVAILABLE` or `ANALYTICS_NOT_CONFIGURED`, render stat cards with `—` and subtitle "Analytics unavailable"; fulfills "Overview page displays four stat cards"
- [x] 9.2 In Overview page, render SVG sparkline chart inside `.panel` below the stat row: map `daily[]` from analytics response to SVG path points (normalize heights to 80px viewBox, use `d` attribute with cubic-bezier smoothing), fill area with `--accent-y-soft`, stroke line with `--ink`, add 3 axis labels; if analytics unavailable render plain text "No data available"; fulfills "Overview page displays 30-day sparkline chart"
- [x] 9.3 In Overview page, render `.panel` for "Recent activity" with a hardcoded `.activity` list of 5 placeholder entries (editor can update these in a future change); fulfills overview page completeness

## 10. Settings Page

- [x] 10.1 Create `frontend/src/views/BackStage/Settings/index.tsx` — render two-panel layout: left panel "Site Information" with `.field` inputs for title, tagline, email, location (content_blocks keys: `settings.title`, `settings.tagline`, `settings.email`, `settings.location`); populate from `usePageContent()` with `portfolio.ts` `meta` values as fallbacks; Save button calls `POST /api/content-blocks`; fulfills "Settings page displays and saves site metadata"
- [x] 10.2 In Settings page, render right panel "Account" with read-only `.activity` list (Plan: BackStage Pro, 2FA: Enabled, Sessions: 1 active, Last password change: computed from JWT `iat`); add "Change password" button that opens change-password modal; fulfills "Settings page displays account information panel"
- [x] 10.3 Implement change-password modal in Settings page: `.modal` with three `.field` inputs (current password, new password, confirm new password); on submit — validate passwords match (inline error if not, no API call), call `POST /api/auth/change-password`, on success close modal + display "Password updated" toast, on 401 display "Current password is incorrect" inline; fulfills "Change-password modal validates and submits"

## 11. Redesign Existing Admin Pages

- [x] 11.1 Rewrite `frontend/src/views/BackStage/Works/index.tsx` — remove all Flowbite imports; wrap table in `.dt-wrap`, use `.dt` with columns: thumbnail `.cell-thumb` (56×36px img), title `.cell-title` + tech tags `.cell-sub`, status `.status-pill.is-pub`/`.is-draft`, view count, updated date, actions `.row-actions` with `.icon-btn` (view, edit, delete); create/edit form in `.modal` using `.field` inputs; delete confirmation in `.modal`; fulfills "Data table component uses .dt-wrap / .dt classes" and "Modal component uses .modal-overlay / .modal classes" and "Form fields use .field / .field__label classes"
- [x] 11.2 Rewrite `frontend/src/views/BackStage/Skills/index.tsx` — remove all Flowbite imports; display skills in `.skill-admin-grid` (2-column at ≥1100px, 1-column below) with `.skill-admin-card` per group; each card shows group title, skill tags with remove buttons, "+ Add skill" button, edit/delete `.icon-btn`; create/edit group in `.modal` with tag-editor field; fulfills "Data table component uses .dt-wrap / .dt classes" for skills management
- [x] 11.3 Rewrite `frontend/src/views/BackStage/Users/index.tsx` — remove all Flowbite imports; wrap in `.dt-wrap` / `.dt`; user rows show avatar circle (initials, `--accent-y` bg), name + email `.cell-sub`, role chip, status `.status-pill`, last seen, `.icon-btn` edit/delete; invite user in `.modal` with name/email/role/status `.field` inputs; fulfills data table and modal specs for users

## 12. Final Integration and Verification

- [x] 12.1 Apply migration to remote D1: `wrangler d1 migrations apply bruno-portfolio-db --remote`; verify tables via `wrangler d1 execute bruno-portfolio-db --remote --command "SELECT name FROM sqlite_master WHERE type='table'"`
- [x] 12.2 Set Wrangler secrets: CF_ZONE_ID set (a215f0f8194d7fc7caba4abfb1bf136c); CF_ANALYTICS_API_TOKEN requires user to create a zone-read token at dash.cloudflare.com and run `echo <token> | wrangler secret put CF_ANALYTICS_API_TOKEN` from workers directory
- [x] 12.3 Deploy updated Workers: deployed via temp ASCII-path workaround (macOS iCloud placeholder issue with unicode project path); verified `GET /api/content-blocks` returns `{}` and `GET /api/experiences` returns `[]` at https://api.jackhellowin.win
- [x] 12.4 Seed initial experience entries: remote `GET /api/experiences` returns the two initial portfolio experience rows so the Content Editor and FrontStage timeline show DB-backed data on first load
- [x] 12.5 Deploy frontend: built with zero errors, deployed via `wrangler pages deploy dist --project-name portfolio --branch main`; production at https://portfolio.jackhellowin.win serves new assets (index-C1q0U6dN.js)
- [x] 12.6 TypeScript build: frontend — 0 errors (`vite build` passes); workers — 0 errors (`tsc --noEmit` passes); tailwind.config.js cleaned of flowbite-react reference
- [x] 12.7 Remote D1 migration verification: Cloudflare token permission issue resolved; `wrangler d1 migrations apply bruno-portfolio-db --remote` reports no pending migrations
