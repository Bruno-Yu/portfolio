## 1. Setup

- [x] 1.1 Install `cobe` in the frontend package for the Use cobe as a restrained FrontStage globe accent decision
- [x] 1.2 Keep `pretext` and `mapcn` absent from dependencies and document the future-only / rejected decisions; keep pretext as a future-only performance option

## 2. FrontStage Globe

- [x] 2.1 Build a Contact globe accent component that initializes `cobe` safely and cleans up animation resources
- [x] 2.2 Place the Contact globe accent near contact-oriented content without changing contact links; place the globe near contact-oriented content
- [x] 2.3 Implement Theme-aware globe styling with existing FrontStage tokens
- [x] 2.4 Implement Motion and viewport resilience for reduced motion, canvas failure, and mobile layout

## 3. Verification

- [x] 3.1 Run frontend typecheck and production build
- [x] 3.2 Start the local frontend server and verify the Contact section visually in light, dark, desktop, and mobile states

Note: Production build passes. Typecheck still reports existing project-wide TypeScript issues outside this change.
